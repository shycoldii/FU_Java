// коллекции - общий интерфейс для хранения данных list linked_list set queue map и тд
collection от него раследуются все перечисленные выше
hash set наследуется от set и тому подобное (tree-set)
linked list наследуется от list
map - словарь
от всех них идут реализации абстрактных классов
из-за того, что они все наследуются от collection, идет полиморфизм и схожие методы (add/удаление/add)

extend - нужен один тип данных
remove передаем один кокретный объект, remove_all - передаем коллекцию
retain - передаем коллекцию, удаляет все элементы первой коллекции, которых нет в переданной коллекции
is_empty() - пустая ли коллекция
to_array() - возвращает массив из нашей коллекции

списки
в java представлены псевдонимы в виде примитивных типов данных
для каждого из них представлен свой отдельный класс
появились они потому, что java работает именно с объектами, поэтому необходимо было создать обертки
Эти примитивные использовать где попало не надо, так как используется много памяти



Обобщенный класс HashSet представляет хеш-таблицу.
Он наследует свой функционал от класса AbstractSet , а также реализует интерфейс Set .
Хеш-таблица представляет такую структуру данных, в которой все объекты имеют уникальный ключ или хеш-код.
HashSet(Collection<? extends E> col): создает хеш-таблицу, в которую добавляет все элементы коллекции col
HashSet(int capacity): параметр capacity указывает начальную емкость таблицы, которая по умолчанию равна 16
HashSet(int capacity, float koef): параметр koef или коэффициент заполнения,
значение которого должно быть в пределах от 0.0 до 1.0, указывает, насколько должна быть заполнена емкость объектами прежде чем произойдет ее расширение.
Например, коэффициент 0.75 указывает, что при заполнении емкости на 3/4 произойдет ее расширение

HashSet<String> states = new HashSet<String>();

        // добавим в список ряд элементов
        states.add("Germany");
        states.add("France");
        states.add("Italy");
        // пытаемся добавить элемент, который уже есть в коллекции
        boolean isAdded = states.add("Germany"); //false